server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://preprod-db:5432/productdb
    username: user_preprod
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
