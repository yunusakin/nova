server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb_test
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
