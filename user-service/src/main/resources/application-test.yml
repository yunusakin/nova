server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/userdb_test
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

nova:
  internal:
    shared-secret: ${NOVA_INTERNAL_SHARED_SECRET:local-internal-secret}
