server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://preprod-db:5432/orderdb
    username: user_preprod
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
